<nav class="navbar navbar-default navbar-inverse">
<div class="container-fluid">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to "IntelliFly", root_path, class: "navbar-brand" %>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav">
      <li><%= link_to t('about'), pages_about_path %></li>
      <li><%= link_to t('contact'), pages_contact_path %></li>
    </ul>
    <!-- replace default search field came with bootstaps with custom one that goes with searchkick -->
    <%= form_tag search_airports_path, method: :get, class: "navbar-form navbar-left" do %>
      <p>
        <%= text_field_tag :search, params[:search], class: "form-control", placeholder: t('lets_find_your_airport') %>
        <%= submit_tag t('search'), name: nil, class: "btn btn-default" %>
      </p>
    <% end %>
    <ul class="nav navbar-nav navbar-right">
      <li class="dropdown">
         <a href="#" class="dropdown-toggle" data-toggle="dropdown">Language <b class="caret"></b></a>
         <ul class="dropdown-menu">
           <li><%= link_to t('english'), root_path(locale: 'en') %></li>
           <li><%= link_to t('vietnamese'), root_path(locale: 'vi') %></li>
         </ul>
       </li>
      <% if user_signed_in? %>
        <li><%= link_to t('edit_profile'), edit_user_registration_path %></li>
        <li><%= link_to t('sign_out'), destroy_user_session_path, method: :delete %></li>
      <% else %>
        <li><%= link_to t('sign_up'), new_user_registration_path %></li>
        <li><%= link_to t('sign_in'), new_user_session_path %></li>
      <% end %>
    </ul>
  </div>
</div>
</nav>
